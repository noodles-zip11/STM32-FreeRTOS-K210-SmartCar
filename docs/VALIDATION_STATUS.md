# 验证状态

本文档用于区分历史硬件观察、当前软件构建结果以及尚未执行的实车验证，避免把文档维护误解为新的硬件验证。

## 历史实车联调

- README 中关于循迹、光照、弯道、调参和系统联调的实测经验，只绑定基线提交 `7ce04845e42bdc2dd3e943514f3bd8fefc8bf97b`。
- 该基线下默认/推荐的实车组合是：STM32 端解析 `0xFF 0xFE` 旧帧，K210 端使用 `k210_v13_anti_glare.py`。
- `k210_v13_basic.py` 使用相同的 `0xFF 0xFE` 旧帧协议，是仓库保留的对比脚本；仓库没有单独记录它与推荐版具有同等范围的实车验证。
- 这些历史记录按原意保留，不代表所有场地、光照、安装条件或参数组合均已验证。

## 当前软件编译验证

2026-09-03 在本机对当前文档维护后的代码树执行：

```sh
cmake --preset Debug
cmake --build --preset Debug
```

结果：

- 构建步骤完成 `54/54`。
- 生成 `build/Debug/freertos_car.elf`。
- RAM：`14648 B / 71.52%`。
- Flash：`54852 B / 83.70%`。

注意：虽然 preset 名称为 `Debug`，当前顶层 `CMakeLists.txt` 会将 `CMAKE_BUILD_TYPE` 强制设置为 `MinSizeRel`。因此上述命令的目录和 preset 名称不表示实际使用 Debug 优化；本记录只是如实记录当前构建配置及结果。

## 未做实车验证

- 2026-09-03 的后续工作仅整理文档，没有重新装车或进行新的上电、行驶、传感器、通信、失效恢复等硬件测试。
- 本次文档维护不扩展基线提交对应的实车验证范围，也不把“编译通过”等同于“实车通过”。
- `k210_v14_final.py` 当前默认发送 `0xFF 0xFD` 扩展帧，而 STM32 当前只解析 `0xFF 0xFE` 旧帧。v14 因此属于未集成实验，未进行端到端实车验证，不能作为 v13 的直接替换。

后续如果修改固件、K210 脚本、协议、硬件接线、机械安装或运行参数，应单独记录对应提交、测试条件与结果。
